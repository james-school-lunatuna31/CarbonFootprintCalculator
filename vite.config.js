
import { defineConfig } from 'vite';

export default defineConfig({
  // Configuration options
  root: '.', // Set the project root directory
  build: {
    outDir: 'dist', // Set the directory for build files
  }
});
